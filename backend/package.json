{
  "name": "cursed-code-reviewer-backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "lint": "eslint . --ext ts",
    "test": "vitest --run",
    "test:watch": "vitest",
    "package": "npm run build && ./package-lambda.sh"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.490.0",
    "@aws-sdk/client-s3": "^3.490.0",
    "@aws-sdk/client-bedrock-runtime": "^3.490.0",
    "@aws-sdk/lib-dynamodb": "^3.490.0",
    "@aws-sdk/util-dynamodb": "^3.490.0",
    "@octokit/rest": "^20.0.2"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.131",
    "@types/node": "^20.10.6",
    "typescript": "^5.3.3",
    "vitest": "^1.2.0"
  }
}
